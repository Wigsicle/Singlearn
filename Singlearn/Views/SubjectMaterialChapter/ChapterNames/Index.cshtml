@model IEnumerable<Singlearn.Models.Entities.ChapterName>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.chapter_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.subject_id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.chapter_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.subject_id)
            </td>
            <td>
                <a asp-action="EditChapterName" asp-route-id="@item.chapter_name_id">Edit</a> |
                <a asp-action="DetailsChapterName" asp-route-id="@item.chapter_name_id">Details</a> |
                <a asp-action="DeleteChapterName" asp-route-id="@item.chapter_name_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

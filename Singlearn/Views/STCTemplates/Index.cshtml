@model IEnumerable<Singlearn.Models.Entities.STCTemplate>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.stc_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.template_id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.stc_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.template_id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.stc_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.stc_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.stc_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
